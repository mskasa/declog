name: ADR Audit

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5.6.0
        with:
          go-version: '1.22'
      - name: Install declog
        run: go install github.com/mskasa/declog@latest
      - name: Run why audit
        id: audit
        run: why audit
      - name: Create GitHub Issue if stale ADRs found
        if: steps.audit.outputs.stale_found == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const title = `[ADR Audit] Stale file references detected (${new Date().toISOString().slice(0, 10)})`;
            const body = process.env.AUDIT_RESULT;
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
            });
            const duplicate = existing.data.find(i => i.title.includes('[ADR Audit]'));
            if (!duplicate) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
              });
            }
